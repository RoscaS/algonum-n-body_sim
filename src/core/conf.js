let conf = {
  minMass: 1000,
  maxMass: 10000000,
  timeStep: 1/70,
  timeMultiplicator: 1,
  trailOpacity: 1,
  mass: 32000,
  damping: 0,

  initialRadius: Math.log(Math.E + 1.0),
  // initialRadius: 2,
};
